## Ride/Traffiq

machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t ride/traffiq .

test:
  override:
    - docker run -d --name amqp rabbitmq; sleep 5
    - docker run --link amqp:amqp -e QUEUE_URL=amqp://guest:guest@amqp:5672 ride/traffiq
